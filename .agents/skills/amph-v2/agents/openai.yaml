interface:
  display_name: "Amph V2"
  short_description: "Repo-specific patterns and workflows for amph-v2"
  default_prompt: "Use the amph-v2 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true